
# TICKET-UT-006

# Extended Edge Case & Regression Test 확장

## Depends On

```text
TICKET-UT-001 ~ UT-005
```

---

## 목표

기본 테스트 구현 이후 boundary case 및 regression scenario를 추가한다.

coverage percentage 자체가 목적이 아니라 실제 장애 가능성이 높은 branch를 보호한다.

---

## DocumentFileOperations 추가 테스트

검토할 항목:

```text
empty path
relative path
filename with multiple dots
filename with spaces
unicode filename
upper/lowercase extension
empty Markdown
large Markdown
invalid destination directory
destination path already exists
```

실제 요구사항에 해당하는 경우만 추가한다.

---

## Markdown Renderer 추가 테스트

```text
heading levels
italic
bold
ordered list
unordered list
link
code block
blockquote
unicode
special characters
empty lines
large document
```

QTextDocument serialization 전체는 비교하지 않는다.

---

## MarkdownRenderState 추가 테스트

```text
연속된 여러 Update Request
invalidate 이후 새로운 Request
complete 이후 새로운 Request
Initial → Update 전환
Rendered HTML 교체
out-of-order callback
```

---

## MarkItDownManager 추가 테스트

```text
stderr only
stdout + stderr
multiple error callbacks
finish 이후 unexpected callback
다시 conversion 실행
unicode stdout
unicode stderr
large stdout
```

실제 implementation에서 의미 있는 경우만 추가한다.

---

## Regression Rule

버그를 발견하면 다음 순서를 따른다.

```text
Bug reproduction
      ↓
Failing test 추가
      ↓
Production fix
      ↓
Test PASS
      ↓
관련 test suite 실행
```

---

## Acceptance Criteria

```text
[ ] 의미 있는 boundary case 추가
[ ] race 관련 regression 테스트 강화
[ ] Unicode 관련 테스트 포함
[ ] filesystem boundary 강화
[ ] process error boundary 강화
[ ] 의미 없는 coverage-only 테스트 없음
[ ] 새 regression case 문서화
```